#include<stdio.h>

int main()
{
    int array[10],i;
    for(i=0;i<10;i++)
    {
        scanf("%d",&array[i]);
    }
    int max;
    scanf("%d",&max)
    max=array[i]
    {
        if
    }
    printf("Maximum number: %d\n",max);
    printf("Numbers in array:");

    for(i=0;i<5;i++)
    {
        printf(" %d",array[i]);
    }
}